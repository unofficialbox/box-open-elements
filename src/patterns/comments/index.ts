// Comments as a standalone concept: the model, then the thread that renders it.
// `box-annotation-thread` (patterns/preview) composes this and adds the
// document anchor.
export * from "./types.js";
export * from "./comment-thread.js";
